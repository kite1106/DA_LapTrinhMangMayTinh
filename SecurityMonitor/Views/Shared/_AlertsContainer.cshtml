<!-- Container cho toast notifications -->
<div id="toastContainer" class="toast-container position-fixed top-0 end-0 p-3" style="z-index: 1050;">
</div>

<!-- Container cho danh sách cảnh báo -->
<div class="alert-container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h4 class="mb-0">
            Cảnh báo 
            <span id="alertCountBadge" class="badge bg-danger">0</span>
        </h4>
    </div>
    <div id="alertsContainer">
        <!-- Các cảnh báo sẽ được thêm vào đây -->
    </div>
</div>

<!-- Include SignalR scripts -->
@await Html.PartialAsync("_RealTimeAlerts")
